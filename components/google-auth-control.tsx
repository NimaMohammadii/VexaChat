"use client";

export function GoogleAuthControl() {
  return (
    <div className="inline-flex items-center gap-2 rounded-full border border-line/80 bg-white/[0.06] px-4 py-2 text-sm font-medium text-paper shadow-[0_8px_30px_rgb(0,0,0,0.2)] backdrop-blur-sm">
      <span className="h-2 w-2 rounded-full bg-emerald-400" aria-hidden />
      <span>Telegram Mini App</span>
    </div>
  );
}
