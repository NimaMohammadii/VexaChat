{
  "name": "vexachat-directory",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "next:build": "prisma generate && next build",
    "build": "npm run cf:build",
    "start": "npm run start:prod:safe",
    "start:prod": "npx prisma migrate deploy && next start",
    "start:prod:safe": "node scripts/db-repair-prisma.js && npx prisma migrate deploy && next start",
    "cf:build": "prisma generate && opennextjs-cloudflare build",
    "cf:preview": "npm run cf:build && wrangler dev",
    "cf:deploy": "npm run cf:build && wrangler deploy",
    "deploy": "npm run cf:deploy",
    "preview": "npm run cf:preview",
    "db:deploy": "npx prisma migrate deploy",
    "postinstall": "prisma generate",
    "db:status": "prisma migrate status",
    "db:repair": "node scripts/db-repair-prisma.js",
    "storage:migrate-supabase-to-r2": "node scripts/migrate-legacy-storage-urls-to-r2.js",
    "storage:migrate-legacy-urls-to-r2": "node scripts/migrate-legacy-storage-urls-to-r2.js",
    "storage:assert-r2-only": "node scripts/assert-r2-only.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.883.0",
    "@aws-sdk/s3-request-presigner": "^3.883.0",
    "@prisma/client": "^6.3.1",
    "@prisma/extension-accelerate": "2.0.2",
    "@supabase/ssr": "^0.5.2",
    "@supabase/supabase-js": "^2.49.8",
    "agora-access-token": "^2.0.4",
    "agora-rtc-sdk-ng": "^4.23.2",
    "framer-motion": "^11.18.2",
    "next": "14.2.35",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "1.8.3",
    "@types/node": "20.14.10",
    "@types/react": "18.3.3",
    "@types/react-dom": "18.3.0",
    "autoprefixer": "10.4.19",
    "eslint": "8.57.0",
    "eslint-config-next": "14.2.5",
    "postcss": "8.4.39",
    "tailwindcss": "3.4.4",
    "typescript": "5.5.3",
    "prisma": "^6.3.1",
    "wrangler": "4.20.5"
  }
}
